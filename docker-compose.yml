version: "2"

services:
  # RabbitMQ
  rmq:
    container_name: rmq
    hostname: rmq
    build: ./docker/rmq
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
    ports:
      - "5672:5672"
      - "15672:15672"
